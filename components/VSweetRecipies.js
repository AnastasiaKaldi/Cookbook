import { Container } from "postcss";
import React from "react";

const VSweetRecipies = () => {
  return (
    <section
      className="vsweetRecipies - [url('Vsweetrecipies.svg')]"
      bg-cover
      bg-center
      bg-fixed
    ></section>
  );
};

export default VSweetRecipies;
